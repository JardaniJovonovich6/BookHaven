spring:
  application:
    name: BookHaven

  datasource:
    url: jdbc:postgresql://localhost:5432/RestRentals
    username: postgres
    password: Aman@1234

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server :
  servlet :
    context-path : /api/v1


